"use client";
import Image from "next/image"; import Link from "next/link"; import { useCallback, useEffect, useState } from "react";
import { getCustomerListHeaders } from "@/components/orders/customerOrderAccess"; import { formatDzd, formatOrderDate, orderStatusClass } from "@/components/orders/orderUtils"; import type { CustomerOrder } from "@/components/orders/types";
type LoadState="loading"|"loaded"|"error";
export function CustomerOrdersClient() { const [orders,setOrders]=useState<CustomerOrder[]>([]); const [cursor,setCursor]=useState<string|null>(null); const [state,setState]=useState<LoadState>("loading"); const [msg,setMsg]=useState<string|null>(null); const [authed,setAuthed]=useState(false);
const load=useCallback(async(next:string|null,mode:"replace"|"append")=>{setState("loading"); setMsg(null); let lastError:unknown=null; for(let attempt=0;attempt<2;attempt+=1){ try{const access=await getCustomerListHeaders(); setAuthed(access.authed); const params=new URLSearchParams(); if(next) params.set("cursor",next); const res=await fetch(`/api/customer/orders?${params}`,{headers: access.headers, cache:"no-store"}); const data=await res.json() as {orders?:CustomerOrder[];nextCursor?:string|null;message?:string}; if(!res.ok||!data.orders) throw new Error(data.message??"Orders could not be loaded."); setOrders((cur)=>mode==="append"?[...cur,...data.orders!]:data.orders!); setCursor(data.nextCursor??null); setState("loaded"); return;}catch(e){lastError=e;}} setState("error"); setMsg(lastError instanceof Error?lastError.message:"Orders could not be loaded.");},[]);
useEffect(()=>{const timer=window.setTimeout(()=>{void load(null,"replace")},0);return()=>window.clearTimeout(timer)},[load]); const loading=state==="loading"; return <section className="customerOrders"><div className="customerOrders__head"><span className="section-number">ORDERS</span><h1>MY ORDERS</h1><p>Secure order history for your 213 RUN checkout sessions.</p></div>{loading?<p className="orderNotice">Loading orders…</p>:null}{state==="error"?<div className="orderEmpty orderEmpty--error"><strong>Orders could not be loaded.</strong><p>{msg}</p><button className="button" onClick={()=>void load(null,"replace")}>RETRY</button></div>:null}{state==="loaded"&&!orders.length?<div className="orderEmpty"><strong>No orders found.</strong><p>{authed?"Your confirmed account orders will appear here.":"Guest orders from this browser appear here. Sign in to keep order history available across devices."}</p><Link href="/shop">CONTINUE SHOPPING →</Link></div>:null}<div className="orderCards">{orders.map((o)=><article className="orderCard" key={o.id}>{o.thumbnail?<Image src={o.thumbnail} alt="" width={76} height={76} unoptimized/>:<span className="orderCard__thumb"/>}<div><strong>{o.orderNumber}</strong><p>{o.items[0]?.name??`${o.itemCount} item${o.itemCount===1?"":"s"}`}</p><small>{formatOrderDate(o.createdAt)}</small></div><span className={orderStatusClass(o.status)}>{o.status}</span><b>{formatDzd(o.totals.totalDzd)}</b><Link href={`/orders/${o.id}`}>VIEW ORDER →</Link></article>)}</div>{cursor?<button className="ordersLoadMore" disabled={loading} onClick={()=>void load(cursor,"append")}>{loading?"LOADING...":"LOAD MORE"}</button>:null}</section> }
