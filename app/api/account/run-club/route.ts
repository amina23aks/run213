import { requireCustomerRequest,CustomerAuthError } from "@/lib/customer-auth";
import { CustomerRunClubIndexError,listOwnedRuns } from "@/lib/run-club/customer";
export const dynamic="force-dynamic";
export async function GET(request:Request){try{const customer=await requireCustomerRequest(request),cursor=new URL(request.url).searchParams.get("cursor");return Response.json(await listOwnedRuns(customer,cursor),{headers:{"Cache-Control":"private, no-store"}});}catch(error){const missingIndex=error instanceof CustomerRunClubIndexError;return Response.json({error:missingIndex?"Run Club activity is temporarily unavailable. Please retry shortly.":error instanceof CustomerAuthError?error.message:"Run Club activity is unavailable.",recoverable:missingIndex},{status:missingIndex?503:error instanceof CustomerAuthError?error.status:500,headers:{"Cache-Control":"private, no-store"}})}}
