rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null && request.auth.token.admin == true;
    }

    match /products/{productId} {
      allow read: if resource.data.status == "active";
      allow write: if isAdmin();
    }

    match /lookCollections/{collectionId} {
      allow read: if resource.data.status == "active";
      allow write: if isAdmin();
    }

    match /looks/{lookId} {
      allow read: if resource.data.status == "active";
      allow write: if isAdmin();
    }


    match /users/{userId}/productFavorites/{productId} {
      allow read, delete: if request.auth != null && request.auth.uid == userId;
      allow create: if request.auth != null
        && request.auth.uid == userId
        && request.resource.data.keys().hasOnly(["itemId", "createdAt"])
        && request.resource.data.itemId == productId
        && request.resource.data.createdAt == request.time;
      allow update: if false;
    }

    match /users/{userId}/lookFavorites/{lookId} {
      allow read, delete: if request.auth != null && request.auth.uid == userId;
      allow create: if request.auth != null
        && request.auth.uid == userId
        && request.resource.data.keys().hasOnly(["itemId", "createdAt"])
        && request.resource.data.itemId == lookId
        && request.resource.data.createdAt == request.time;
      allow update: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
